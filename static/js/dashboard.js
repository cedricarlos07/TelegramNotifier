document.addEventListener('DOMContentLoaded', function() {
    // Chart initialization code can be added here if needed for the dashboard
    // For now, it's empty as the dashboard uses simple HTML layout
});
