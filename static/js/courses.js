document.addEventListener('DOMContentLoaded', function() {
    // This file would contain more complex JavaScript for course management if needed
    // For now, the functionality is handled inline in the HTML template
});
